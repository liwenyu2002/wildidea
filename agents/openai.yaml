interface:
  display_name: "WildIdea"
  short_description: "Generate cross-domain anchors and a 16:9 HTML poster"
  default_prompt: "Use $wildidea to generate concrete cross-domain anchors and mechanism-transfer candidates, then create a 16:9 HTML poster for this product, strategy, research, or algorithm question."

interface:
  display_name: "WildIdea"
  version: "1.3"
  short_description: "Generate far-domain idea cards with search, judging, and HTML output"
  default_prompt: "Use $wildidea to draw distant-domain source mechanisms, run live novelty search, score candidates with an independent judge, and return a validated HTML idea-card page."
